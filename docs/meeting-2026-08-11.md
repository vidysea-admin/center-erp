# 2026-08-11 Boss Meeting — Requirements & Delivery

Source: meeting transcription (boss + Umesh + Manish, ~19 min, two recordings).
Boss's priority: **everything up to batch management now; costing, invoicing and reporting
explicitly Phase 2.** All of the below shipped the same day; suites green
(e2e 228 · sync 33 · roles 40 = **301 assertions**).

## What was asked → what was built

| # | Ask (from the meeting) | Delivered |
|---|---|---|
| 1 | Client updates their sheet directly and tells nobody — track **every tab, every column** every 30–60 min, highlight old → new per row/column, review by Admin/Manish | **Sheet Watch**: watch-mode SyncSource polls every `interval_minutes` (default 30), snapshots all tabs, diffs cell-by-cell, `/sheet-watch` review page (New/Seen/Accepted), sidebar badge, 48h-stale alert. OneDrive share links fetched anonymously via the badger-token flow — no credentials stored. `scripts/setup-watch-source.mjs` registers the real Vidysea-RPL workbook. TC Password column masked for non-Admins. |
| 2 | Multiple SPOCs / contact persons per location | `contacts[]` on Location + editor on the new **Contacts & Notes** tab (legacy spoc/principal fields untouched) |
| 3 | Meeting notes per location — who/when/what, so boss & Manish stay on the same page | `MeetingNote` model + Notes list/add on the same tab, author + date recorded, audited |
| 4 | Trainer journey: application pool → shortlist → TOT → **Ready to Train**; NSDC TR ID; serves 1/2/10 locations; compensation batch-wise/monthly + fixed/incentive | `pipeline_status`, `tr_id`, `capable_locations[]`, `programs_applied[]`, `compensation_type` + `compensation_fixed` on Trainer, all editable in the Trainers UI. Booking a not-yet-Ready trainer **warns** (Rule 11's availability dates still hard-gate the start) |
| 5 | Candidate eligibility: age 18–40, 10th pass, no training in last 6 months (capture last-training date) — "पांच-सात criteria" | `candidateEligibility()` computed on read from Defaults (`min_age`, `max_age`, `training_cooldown_months`); badge on the candidates list; **warn on assign, hard-block on enrollment completion**. Unknown DOB/education never blocks — shown as "Unverified" |
| 6 | SIDH registration a candidate-level stage; "send a link so बच्चा खुद register कर ले" | `sidh_status` (Not Registered / Link Sent / Registered) + **Send link** button (WhatsApp deep link, URL from Defaults `sidh_url` = skillindiadigital.gov.in) + ✓ Registered tick |
| 7 | Assisted registration when the candidate can't self-register | **Export for SIDH CRM** — xlsx in the `D:\crm` console's column contract (`full_name, mobile, aadhaar_or_vid, qp_code, scheme_id, district`; aadhaar blank by design — the ERP never stores Aadhaar) |
| 8 | Public self-registration link ("मैंने आपको link भेज दिया, आप detail भर दो") | `/p/register/<token>` — per-location capability link (Self-reg link button on Candidates), mobile-first, honeypot + per-IP rate limit, revocable, duplicate-check advisory |
| 9 | **Backward batch planner** — start date → registration D-1, mobilization D-2, TOT ≥D-3, all lead times configurable, shareable, markable | `planBatchBackward()` from Defaults lead-times; every new batch stores a `milestones[]` checklist (tick-off, regenerate while Planning, overdue alert); standalone **Plan a batch** calculator with copy-as-text + print |
| 10 | Trainer parallel batches: **up to 4** with time division ("7–11, 12–4, 5–9") | Cap 4 (Defaults + Trainer default + migration `migrate-policy-2026-08-11.mjs`); optional `slot_start`/`slot_end` on Batch; same-trainer overlapping time slots hard-blocked |
| 11 | Photos/videos at least 2×/day with timestamps, else notify | `min_daily_evidence` default 2 in Defaults; `evidence_low` alert on yesterday's log for Active batches |
| 12 | Candidate feedback ("हर बच्चा… feedback दे पाए") | `/p/feedback/<token>` — one link per roster member (generated from the batch's new **Feedback** tab, WhatsApp share), 1–5 stars + liked + suggestions, one response per member, average shown to Admin/Ops/SPOC |

## Deliberately deferred (boss said so, or gated)

- Costing / invoicing / margin — boss: "second phase में करेंगे"
- Reporting — boss: "अभी reporting पे मत दिमाग लगाओ"
- Govt attendance file parsing — needs the sample files boss has ("बहुत सारे sample हैं")
- Native app — web app suffices per the meeting; candidate/trainer flows are mobile-first pages
- Full candidate login — tokenized links ship now; real auth if feedback volume demands it

## Open items

1. Govt time-slot legality (3×3hr batches?) — **Manish is verifying**; the ERP stores slots and
   clash-checks regardless, so no rework either way.
2. Google planner sheet (back-date Excel) still not shared with himanshu@vidysea.com — planner
   was built from the meeting's own rules; the sheet would only validate the lead-time defaults.
3. `scheme_id` for the SIDH CRM export is left blank per row (observed value `3` once, not
   proven constant) — operator confirms in the CRM console.

## Ops notes

- Run once per environment: `node --env-file=.env.local scripts/migrate-policy-2026-08-11.mjs`
  (cap 5→4) and `node --env-file=.env.local scripts/setup-watch-source.mjs` (registers the
  OneDrive watch source).
- The watch poller ticks inside the existing locked scheduler — multi-container safe.
