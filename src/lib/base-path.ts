// Single source of truth for the app's URL prefix. Must match next.config.ts basePath.
// next/link and next/router prefix automatically; raw fetch()/window.open/stored URLs use this.
export const BASE_PATH = "/erp";
